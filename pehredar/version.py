"""Single source of truth for the Pehredar Python core version.

Keep in sync with ``pyproject.toml`` (``[project] version``) and
``gui/package.json`` (``version``) when cutting a release.
"""

__version__ = "1.0.0"
